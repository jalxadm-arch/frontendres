services:
  - type: web
    name: restaurante-sierra
    env: node
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://lasierraerestaurant.com/api
